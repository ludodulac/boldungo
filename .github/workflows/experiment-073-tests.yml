name: Experiment 073 qualitative world hypothesis
on:
  push:
    branches: [experiment/qualitative-world-hypothesis-073]
  pull_request:
    branches: [experiment/ingest-global-connectivity-072]
jobs:
  targeted-tests:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
      - run: python -m pip install -e ".[dev]"
      - run: pytest -q -s tests/vision/test_multiview_workspace.py -k "073 or 072 or 071 or 070 or 069 or 068 or 067 or 066 or 065 or 064"
